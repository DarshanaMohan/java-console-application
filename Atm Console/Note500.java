public class Note500 extends Notes// The Note500 class is a subclass of the Notes class
{
    public Note500(int note,int count) {// Constructor to initialize the denomination (500)
        super(note, count);// Calling the superclass constructor (Notes) to initialize the note denomination and count
    }
}

