public class Note200 extends Notes// The Note200 class is a subclass of the Notes class
{
    public Note200(int note,int count)// Constructor to initialize the denomination (200)
    {
        super(note,count);// Calling the superclass constructor (Notes) to initialize the note denomination and count

    }
}

