public class Note2000 extends Notes // The Note2000 class is a subclass that extends the Notes class
{
    public Note2000(int note,int count)// Constructor to initialize the denomination (2000)
    {
        super(note,count);// Calling the superclass constructor (Notes) to initialize the note denomination and count
    }
}