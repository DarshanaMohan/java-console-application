public class Note100 extends Notes// The Note100 class is a subclass of the Notes class
{
    public Note100(int note,int count)// Constructor to initialize the denomination (100)
    {
        super(note,count);// Calling the superclass constructor (Notes) to initialize the note denomination and count
    }
}

